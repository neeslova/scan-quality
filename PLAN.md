# PLAN.md — Система оценки качества сканов с распознаванием дефектов

> Рабочий документ проекта. Держим открытым в VS Code, обновляем чек-листы по ходу.
> Ассистенту: перед задачей читаем разделы «Ограничения», «Соглашения» и текущий спринт.
> Все решения фиксируем в разделе «Журнал решений» внизу — не переспрашиваем то, что там записано.

---

## 1. Цель

Приложение на Python, которое принимает скан документа и автоматически выдаёт:

1. **Вердикт**: `good` / `acceptable` / `bad`
2. **Список дефектов** с вероятностями (multi-label)
3. **Локализацию** дефекта на изображении (heatmap)
4. **Машиночитаемый JSON** — чтобы систему можно было встроить в автоматическую проверку потока сканов

Интерфейс: Gradio (одиночный режим + батч) и CLI-режим для обработки папки.

## 2. Ограничения и вводные

| Параметр | Значение | Следствие для проекта |
|---|---|---|
| Обучение | Google Colab (T4) | Код запускается через CLI из тонкого ноутбука, чекпоинты на Drive, resume обязателен |
| Инференс | Локальный CPU | Экспорт в ONNX, backbone не тяжелее EfficientNet-B0 / ConvNeXt-tiny |
| LLM | **Не требуется** | Ядро — CNN + CV-метрики. Текстовый отчёт через внешний API — опциональный модуль с флагом |
| Датасет | **Tobacco3482** (3482 реальных офисных скана, MIT), разметки качества нет | Ручная разметка 600–800 шт. + синтетика поверх чистых реальных |
| Второй домен | **YeniseiGovReports-TD** (1799 сканов, кириллица, MIT) | Отложенная проверка обобщения: обучаемся на одном корпусе, меряем на другом |
| Тип документов | Офисные документы: служебки, письма, факсы, формы, отчёты | Патчи из полного разрешения; дефекты сканера, не фотосъёмки |
| Язык | Основной корпус — английский, проверочный — русский | OCR запускается с языком под корпус (`eng` / `rus`), тексты отчётов на русском |
| Оцифровка | Планшет/микрофильм: **кадр = страница** | Фото документа на столе система не поддерживает — нужен отдельный этап детекции страницы, его в объёме нет |

**Критично для автоматической проверки:** система не должна зависеть от внешней сети. Внешний API допустим только как декоратор над готовым результатом, никогда — как источник вердикта.

## 3. Архитектура

```
скан (jpg/png/pdf)
   │
   ├─► препроцессинг: загрузка, деskew-детект, нарезка на патчи 384×384 (сетка ~6×9 для A4@300dpi)
   │
   ├─► [A] CV-метрики (детерминированные, без обучения)
   │        Laplacian var, Tenengrad, доля и кластеры пересвета,
   │        угол перекоса (Radon/Hough), RMS-контраст, оценка высоты строки, уровень шума
   │
   ├─► [B] CNN multi-label (timm, 10 сигмоид)
   │        инференс по патчам → агрегация: max для локальных, mean для глобальных дефектов
   │
   ├─► [C] OCR-слой (EasyOCR/Tesseract rus)
   │        средняя confidence, доля «мусорных» символов, плотность распознанного текста
   │
   └─► агрегатор → QualityReport (pydantic)
            вердикт по правилу с калиброванными порогами
            + Grad-CAM heatmap
            + (опц.) текстовое объяснение через внешний API
```

**[A] — baseline.** Обязателен: в записке нужно сравнение «CV-метрики vs CNN vs CNN+OCR».
**[B] — ядро.** Multi-label, `BCEWithLogitsLoss` + `pos_weight` под дисбаланс.
**[C] — источник метки `unreadable`** и практическое обоснование пользы системы.

### Почему патчи, а не ресайз всей страницы

A4 в 300 dpi — это 2480×3508. При ресайзе в 224×224 текст превращается в серый шум, и `blur` / `low_resolution` становятся физически недетектируемыми. Поэтому:

- **train:** случайные патчи 384×384 из исходного разрешения, метки наследуются от изображения (для глобальных дефектов) или от маски деградации (для локальных)
- **inference:** регулярная сетка патчей + агрегация

## 4. Таксономия дефектов

Multi-label (дефекты сочетаются), 10 меток:

| Метка | Определение | CV-сигнал | Как синтезировать |
|---|---|---|---|
| `blur` | Расфокус или смаз, края символов размыты | низкая дисперсия Лапласиана, Tenengrad | GaussianBlur, MotionBlur (Albumentations) |
| `glare` | Локальный пересвет, участок выбит в белое | кластеры пикселей >245 с площадью > порога | радиальный/эллиптический градиент яркости |
| `shadow` | Затемнение от сгиба, переплёта, края крышки | градиент яркости по краю, локально низкая медиана | Augraphy `ShadowCast`, `LightingGradient` |
| `skew` | Перекос строк > 2° | пик Radon-преобразования вне 0° | affine rotation |
| `cropped` | Обрезан край документа, часть текста вне кадра | отсутствие полей, текст вплотную к границе | случайный crop края |
| `low_resolution` | Высота строки < ~15 px, символы неразличимы | оценка высоты строки, спектр высоких частот | downscale → upscale |
| `low_contrast` | Блёклая печать, серый текст на сером фоне | RMS-контраст, узкая гистограмма | сжатие диапазона яркости, gamma |
| `noise` | Шум, зерно, грязь на стекле сканера | оценка σ шума, высокочастотная энергия | GaussNoise, ISONoise, соль-перец |
| `streaks` | Полосы, следы валиков, артефакты копира | периодичность по столбцам/строкам | Augraphy `DirtyRollers`, `BadPhotoCopy` |
| `unreadable` | Текст не распознаётся | OCR confidence + доля мусорных символов | производная метка, ставится автоматом по [C] |

`unreadable` — **не отдельный дефект, а следствие**. Размечается автоматически, в обучении участвует как отдельный выход, но в правиле вердикта имеет наибольший вес.

### Правило вердикта

Пороги калибруются по PR-кривым (спринт 6), не подбираются на глаз. Целевая асимметрия: **пропустить плохой скан хуже, чем отклонить хороший**, поэтому по `unreadable` и `blur` целимся в recall ≥ 0.95.

```
bad         := unreadable > τ_u  ИЛИ  любой дефект > τ_high
acceptable  := есть дефекты в диапазоне (τ_low, τ_high]
good        := все метки ≤ τ_low
```

## 5. Структура репозитория

```
scan-quality/
├── PLAN.md                  ← этот файл
├── README.md
├── pyproject.toml           # pip install -e .
├── requirements.txt
├── configs/
│   ├── base.yaml            # единая точка правды: метки, пороги, гиперпараметры, пути
│   └── corpora/             # оверлеи под корпус: только то, что отличается
│       └── yenisei.yaml     #   (обычно блок cv.scores — якоря не универсальны)
├── notebooks/
│   ├── train.ipynb          # ТОНКИЙ: clone → unpack → python -m src.models.train
│   └── eda.ipynb
├── src/
│   ├── __init__.py
│   ├── config.py            # загрузка yaml → pydantic-схема
│   ├── imaging.py           # общие операции: локальная бинаризация, σ шума, local std
│   ├── io/
│   │   ├── loader.py        # jpg/png/tiff/pdf → np.ndarray, нормализация dpi
│   │   └── patches.py       # нарезка на сетку, отбор патчей с текстом, агрегация
│   ├── metrics/
│   │   ├── blur.py          # Laplacian var, Tenengrad
│   │   ├── glare.py         # кластеры пересвета (яркое + плоское + ярче бумаги)
│   │   ├── shadow.py        # карта освещённости, доля затенённой площади
│   │   ├── geometry.py      # skew (профиль проекции), поля документа
│   │   ├── contrast.py      # RMS, диапазон, разрыв бумага/чернила
│   │   ├── noise.py
│   │   ├── streaks.py       # аномальные колонки/строки фона
│   │   ├── text_scale.py    # оценка высоты строки
│   │   ├── baseline.py      # сборка метрик страницы → скоры по меткам
│   │   └── calibrate_anchors.py  # подбор якорей по перцентилям корпуса
│   ├── ocr/
│   │   ├── engine.py        # обёртка над EasyOCR/Tesseract rus
│   │   └── readability.py   # confidence, доля мусора, метка unreadable
│   ├── labeling/
│   │   ├── app.py           # Gradio-разметчик
│   │   └── prelabel.py      # черновые метки из CV-метрик
│   ├── data/
│   │   ├── split.py         # сплит ПО ДОКУМЕНТАМ, не по страницам
│   │   ├── degrade.py       # деградации + маски локальных дефектов
│   │   ├── generate.py      # синтетика поверх чистых реальных
│   │   └── dataset.py       # torch Dataset, аугментации
│   ├── models/
│   │   ├── model.py         # timm backbone + multi-label head
│   │   ├── train.py         # CLI: --config --data --out --resume
│   │   ├── evaluate.py      # per-label PR/F1, кросс-валидация
│   │   ├── calibrate.py     # подбор порогов по PR-кривым
│   │   ├── gradcam.py
│   │   └── export_onnx.py
│   ├── pipeline.py          # оркестратор: image → QualityReport
│   ├── schema.py            # pydantic: QualityReport, DefectScore
│   ├── explain.py           # опц. текстовый отчёт через внешний API, флаг --explain
│   ├── app.py               # Gradio-приложение
│   └── cli.py               # батч-обработка папки → CSV/JSON
├── data/                    # в .gitignore
│   ├── raw/                 # исходные реальные сканы
│   ├── labeled/             # labels.jsonl после ручной разметки
│   ├── synthetic/
│   └── splits/              # train/val/test.json (список id документов)
├── models/                  # чекпоинты, quality.onnx — в .gitignore
├── reports/                 # графики, таблицы метрик для записки
└── tests/
```

## 6. Конфиг (`configs/base.yaml`)

Никаких магических чисел в коде — всё сюда.

```yaml
labels: [blur, glare, shadow, skew, cropped, low_resolution,
         low_contrast, noise, streaks, unreadable]

data:
  patch_size: 384
  target_dpi: 300
  grid: {rows: 9, cols: 6}
  aggregation:            # как схлопывать предсказания по патчам
    local:  [glare, shadow, cropped, streaks]      # max
    global: [blur, low_resolution, low_contrast, noise, skew, unreadable]  # mean

model:
  backbone: efficientnet_b0
  pretrained: true
  dropout: 0.2

train:
  epochs: 30
  batch_size: 32
  lr: 3.0e-4
  optimizer: adamw
  scheduler: cosine
  seed: 42
  pos_weight: auto        # считать по частотам в train

verdict:
  tau_low: 0.30           # калибруется в calibrate.py, здесь — начальные значения
  tau_high: 0.60
  tau_unreadable: 0.50
```

## 7. Стратегия данных

### 7.1 Порядок операций (нарушать нельзя)

1. Ручная разметка реальных → `labels.jsonl`
2. **Сплит по документам** → `splits/{train,val,test}.json`
3. Синтетика — генерируется **отдельно для каждой части**, из эталонов только этой части
4. Обучение
5. Тест — один раз, в самом конце, только на реальных из `test.json`

Причина сплита по документам: у договора страницы почти идентичны (та же бумага, тот же прогон сканера). Случайное перемешивание страниц = утечка, метрики завышаются на 10–15%.

### 7.2 Бюджет

| Что | Объём |
|---|---|
| Ручная разметка реальных | **300** (~1.5 часа) — см. решение №28 |
| Из них в отложенный тест | 150 (сплит 0.2 / 0.3 / 0.5) |
| Эталоны для деградаций | отбираются по CV-скору из документов ВНЕ val и test |
| Синтетика (6–8 деградаций на эталон) | 8–15 тыс. |

### 7.3 Разметчик

Минимальный Gradio-интерфейс: изображение + zoom + 10 чекбоксов + кнопки «дальше/назад» + горячие клавиши + автосохранение в JSONL после каждого изображения. Предзаполнение чекбоксов черновыми метками из `prelabel.py` — ускоряет работу вдвое.

### 7.4 Синтетика

Не генерируем документы с нуля — деградируем **чистые реальные сканы**. Домен сохраняется (те же бланки, шрифты, печати, фон бумаги), метки известны по построению. Для локальных дефектов (`glare`, `shadow`, `streaks`, `cropped`) сохраняем **маску области** — она нужна для корректной разметки патчей.

Контроль частот: следим, чтобы редкие метки встречались не реже ~15% выборки, иначе `pos_weight` не спасёт.

## 8. Работа с Colab

**Правило: в ноутбуке нет логики.** Весь код в `src/`, ноутбук — запускалка.

```python
# notebooks/train.ipynb — весь ноутбук
from google.colab import drive; drive.mount('/content/drive')
!git clone https://github.com/<user>/scan-quality.git
!cd scan-quality && pip install -e . -q
!mkdir -p /content/data && tar -xf /content/drive/MyDrive/scanq/dataset.tar -C /content/data
!cd scan-quality && python -m src.models.train \
    --config configs/base.yaml \
    --data /content/data \
    --out /content/drive/MyDrive/scanq/runs \
    --resume auto
```

Обязательные требования к `train.py`:

- сохранение чекпоинта **каждую эпоху** на Drive + `--resume auto` (сессия отвалится, это вопрос времени)
- копирование использованного конфига рядом с чекпоинтом
- лог метрик в CSV на Drive
- фиксированный seed, детерминированные аугментации при валидации

Данные — **одним архивом** в Drive, распаковка в `/content`. Чтение тысяч мелких файлов напрямую с Drive медленнее самого обучения.

Локально ставим CPU-версию torch (лёгкая, для отладки и инференса), в Colab torch уже есть с CUDA.

## 9. Спринты

### С0 — каркас `[x]`
- [x] `pyproject.toml`, `requirements.txt`, `.gitignore`, venv `.venv`, `pip install -e ".[dev]"`
- [x] структура папок, `configs/base.yaml`, `src/config.py`, `src/schema.py`
- [x] заглушка `src/app.py` + `src/pipeline.py`: картинка → фиктивный `QualityReport` в JSON
- [x] `notebooks/train.ipynb` по шаблону выше
- [x] ruff + black в `pyproject.toml`, `tests/test_smoke.py` (13 проверок)
- **DoD выполнен:** `python -m src.app` поднимает Gradio (`GET /` → 200), `--image` печатает
  JSON, 13 тестов зелёные, ruff/black чисто

### С1 — CV-метрики (baseline) `[x]`
- [x] `src/metrics/*` — 8 модулей + `baseline.py`, все параметры в `configs/base.yaml` (секция `cv`)
- [x] `src/io/loader.py` (jpg/png/tiff/pdf, нормализация dpi, кириллица в пути),
      `src/io/patches.py` (сетка, отбор патчей с текстом, агрегация max/mean)
- [x] подключение метрик в `pipeline.py`, вывод вероятностей и таблицы метрик в Gradio
- [x] unit-тесты: 13 деградаций, каждая сдвигает свою метрику; 46 тестов зелёные
- **DoD выполнен:** на A4@300dpi чистый скан → `good` 0.95; blur/glare/shadow/skew →
      `bad` с правильной меткой 1.00; ~1.2 с на страницу на CPU

**Что калибровка вскрыла** (все чинилось по ходу, детали в журнале решений №13–27):
глобальный Оцу разваливается на тени, порог яркости 245 объявляет бликом чистую
белую бумагу, Tenengrad без нормировки путает блёклую печать с расфокусом,
нормализация dpi вверх сама производит размытие, а тёмный край скана схлопывает
страницу в одну «строку» высотой в лист.

**Внимание при чтении цифр.** Вердикты `good/acceptable/bad` пока не осмысленны:
пороги `tau_low = 0.30` и `tau_high = 0.60` — начальные значения из раздела 6,
их никто не калибровал. При этом якоря ставят каждую метку как ранг внутри корпуса,
то есть у ~5% страниц метка по построению уходит в 1.0; при девяти метках
объединение накрывает большинство страниц, и доля `bad` получается высокой
механически. Пороги вердикта считаются по PR-кривым в С6, когда появятся метки.

### С2 — разметка `[~]`
- [x] `src/labeling/prelabel.py` — черновые метки по CV-метрикам, отбор очереди,
      пул процессов, продолжение с места обрыва
- [x] `src/labeling/app.py` — разметчик: предзаполненные чекбоксы, горячие клавиши,
      дозапись в JSONL после каждой страницы
- [ ] **ручная разметка 600–800 изображений** — это работа человека
- [x] `src/data/split.py` — сплит по документам с выравниванием долей по страницам,
      проверка на утечку, частоты меток по частям
- [ ] отчёт по частотам меток в `reports/` — после разметки
- **DoD:** `labels.jsonl` + три файла сплита; тест отложен и не открывается до С8

Порядок запуска:

```powershell
# 1. черновые метки и очередь (идёт минутами, продолжается с места обрыва)
python -m src.labeling.prelabel --data data\raw\tobacco3482\data `
    --corpus-name tobacco3482 --out data\labeled\prelabels_tobacco.jsonl `
    --queue data\labeled\queue_tobacco.json

# 2. разметка руками
python -m src.labeling.app --data data\raw\tobacco3482\data `
    --queue data\labeled\queue_tobacco.json --labels data\labeled\labels.jsonl

# 3. сплит
python -m src.data.split --labels data\labeled\labels.jsonl --out data\splits
```

### С3 — OCR-слой `[ ]`
- [ ] `src/ocr/engine.py` (EasyOCR `rus`, fallback Tesseract)
- [ ] `src/ocr/readability.py` — confidence, доля мусорных символов, плотность текста
- [ ] автоматическая метка `unreadable`, ручная сверка на 50 примерах
- [ ] график корреляции CV-метрик с деградацией OCR → `reports/`
- **DoD:** метка `unreadable` проставлена по всему размеченному набору, корреляционный анализ готов

### С4 — синтетика `[ ]`
- [ ] `src/data/degrade.py` — 9 деградаций + маски локальных
- [ ] `src/data/generate.py` — генерация отдельно по частям сплита, контроль частот
- [ ] визуальный контроль: сетка по 10 примеров на каждый дефект → `reports/`
- [ ] упаковка датасета в tar для Drive
- **DoD:** 8–15 тыс. изображений с метками, глазами проверено, что «блик» выглядит как блик

### С5 — обучение `[ ]`
- [ ] `src/data/dataset.py`, `src/models/model.py`, `src/models/train.py`
- [ ] запуск в Colab, resume проверен принудительным обрывом
- [ ] `src/models/evaluate.py` — per-label PR/F1 на val
- [ ] 5-fold кросс-валидация, результат со средним и разбросом
- **DoD:** macro-F1 на val устойчиво выше CV-baseline; чекпоинт и конфиг на Drive

### С6 — калибровка и экспорт `[ ]`
- [ ] `src/models/calibrate.py` — пороги по PR-кривым под целевой recall
- [ ] `src/models/export_onnx.py` + проверка расхождения torch vs onnx < 1e-3
- [ ] замер времени инференса на CPU для страницы A4
- **DoD:** `models/quality.onnx` работает через onnxruntime, пороги записаны в конфиг

### С7 — приложение `[ ]`
- [ ] Gradio: одиночный режим, батч, PDF, вердикт, вероятности, Grad-CAM
- [ ] `src/cli.py` — обработка папки → CSV/JSON
- [ ] `src/explain.py` — опциональный текстовый отчёт, флаг `--explain`, graceful fallback
- **DoD:** папка из 50 сканов обрабатывается одной командой без интернета

### С8 — оценка и документация `[ ]`
- [ ] прогон на отложенном тесте (единственный раз)
- [ ] сравнительная таблица: CV-baseline / CNN / CNN+OCR
- [ ] разбор 20 худших ошибок с картинками
- [ ] README, пояснительная записка, схема архитектуры

## 10. Метрики для защиты

1. **Per-label**: precision, recall, F1, AP по каждому из 10 дефектов + macro-F1
2. **Вердикт**: accuracy, F1, confusion matrix на реальном тесте
3. **Практическая польза**: корреляция Спирмена между предсказанным скором качества и фактической ошибкой OCR — доказывает, что «плохой скан» по мнению системы действительно плохо распознаётся
4. **Ablation**: CV-baseline → CNN → CNN+OCR, вклад каждого компонента
5. **Производительность**: время инференса на CPU и GPU, размер модели
6. **Кросс-валидация**: 5 фолдов, среднее ± std

## 11. Соглашения по коду

- Python 3.9+ (локально стоит 3.9.13, в Colab 3.11) — значит `from __future__ import annotations`
  и `typing.List/Optional/Union`, без синтаксиса `X | Y` в аннотациях
- type hints обязательны в публичных функциях
- Все параметры — через `configs/*.yaml` и argparse, никаких констант в теле функций
- Каждый модуль в `src/metrics/` и `src/data/` — чистые функции, без глобального состояния (иначе не тестируется)
- `pydantic` для всех структур на границах: `QualityReport`, конфиг, результат OCR
- Логирование через `logging`, не `print`
- ruff + black перед коммитом
- Коммиты по спринтам, ветка на спринт
- В `.gitignore`: `data/`, `models/`, `.venv/`, `runs/`, `.claude/`

## 12. Риски

| Риск | Проявление | Что делаем |
|---|---|---|
| Утечка через страницы одного документа | Метрики завышены на 10–15% | Сплит по документам, синтетика после сплита |
| Синтетика ≠ реальность | Модель выучит артефакты генератора | Тест только на реальных, разнообразие деградаций, домен от реальных эталонов |
| Ресайз убивает мелкие дефекты | `blur`, `low_resolution` не детектируются | Патчи из полного разрешения |
| Дисбаланс редких меток | `streaks`, `cropped` не выучиваются | Контроль частот при генерации, `pos_weight`, отдельный анализ per-label recall |
| Обрыв сессии Colab | Потеря обучения | Чекпоинт каждую эпоху на Drive, `--resume auto` |
| Маленький тест | Широкий доверительный интервал | 5-fold кросс-валидация, приводить разброс |
| Разметка съедает время | Срыв сроков | Предразметка автоматом, разметчик с горячими клавишами, лимит 800 изображений |

## 13. Журнал решений

| № | Решение | Обоснование |
|---|---|---|
| 1 | Ядро — CNN multi-label, не LLM/VLM | Дефекты сканов — статистика пикселей, не семантика. CNN точнее, быстрее, обучается на T4 |
| 2 | Внешний API — только для текстового отчёта, опционально | Автоматическая проверка не должна зависеть от сети |
| 3 | Синтетика поверх реальных чистых, не генерация с нуля | Совпадение домена: те же бланки, шрифты, бумага |
| 4 | Обучение и инференс на патчах 384×384 | При ресайзе A4 в 224px текст исчезает |
| 5 | Сплит по документам | Страницы одного договора почти идентичны |
| 6 | `moire` заменён на `streaks` | Домен — сканы А4 с МФУ, не фото экрана |
| 7 | `unreadable` размечается автоматом по OCR | Объективный ground truth вместо субъективной оценки |
| 8 | Асимметричные пороги, recall > precision | Пропустить плохой скан дороже, чем отклонить хороший |
| 9 | Целевой Python 3.9, а не 3.10+ | Локально стоит 3.9.13; ставить второй интерпретатор ради `X \| Y` не окупается |
| 10 | Конфиг с `extra="forbid"`, валидация покрытия меток в `aggregation` | Опечатка в yaml должна падать при старте, а не тихо менять поведение обучения |
| 11 | Пины `pydantic<2.10` и `huggingface-hub<1.0` | Gradio 5 требует Python 3.10+, значит сидим на 4.44.1; она несовместима с pydantic 2.10+ (падает генерация API-схемы) и с hub 1.0 (удалён `HfFolder`). Снимать пины — только вместе с переездом на Python 3.10+ |
| 12 | `GRADIO_ANALYTICS_ENABLED=False` при старте приложения | Gradio при импорте ходит в api.gradio.app, а система обязана работать без сети |
| 13 | Бинаризация чернил — локальный порог, а не Оцу; отступ растёт вместе с σ шума | Тень сдвигает глобальный порог, и затемнённая половина страницы уходит в «текст»: поля схлопываются, чистый скан помечается как `cropped`. Фиксированный отступ на зернистом скане склеивает все строки в одну — высота строки становится равной высоте страницы |
| 14 | `blur` считается по `tenengrad_norm` = Tenengrad / (разрыв бумага-чернила)² | Градиент пропорционален перепаду яркости: без нормировки блёклая печать даёт тот же провал резкости, что и расфокус, и `low_contrast` неотличим от `blur` |
| 15 | ~~`low_resolution` меряется в пикселях ИСХОДНОГО файла~~ **отменено решением №25** | Было верно, пока загрузчик растягивал сканы вверх. После запрета апскейла (№20) скан ниже целевого dpi остаётся мелким сам по себе |
| 16 | Блик = «ярче порога» И «ярче собственной бумаги» И «без фактуры» | Одного порога 245 мало: белая бумага сама ярче, и любой чистый скан получал `glare` = 1.0. Блик — это потеря информации, поэтому проверяется ещё и локальная дисперсия |
| 17 | Резкость и шум — по патчам с текстом, геометрия и освещённость — по всей странице | На пустом поле бумаги краёв нет, и метрика резкости на нём всегда скажет «размыто» |
| 18 | Основной корпус — Tobacco3482 (англ.), проверочный — YeniseiGovReports (рус.) | Дефекты сканов — статистика пикселей, а не язык (решение №1), поэтому обучение на англоязычном корпусе допустимо. Проверка на русском корпусе превращает это утверждение из декларации в измерение и даёт ablation для записки |
| 19 | Фотографии документов (`data/gen_photos`) в работу не берём | На них кадр ≠ страница: тёмный фон вокруг листа бинаризуется как чернила, поля обнуляются, Оцу делит гистограмму на «лист/стол». CV-baseline дал 80/80 `bad`. Нужен отдельный этап детекции страницы, которого в объёме нет |
| 20 | Нормализация dpi — только вниз (`data.allow_upscale: false`) | Растягивание скана 196 dpi до 300 сглаживает края букв интерполяцией: загрузчик сам производит размытие. На Tobacco это давало `blur` на 59 сканах из 60 |
| 21 | На битональном скане `low_contrast` и `noise` не выдаются вовсе | Разрыв бумага/чернила там всегда максимален, шум обнулён бинаризацией. Ноль от отсутствия шкалы нельзя подавать как ноль от отсутствия дефекта — для этого в отчёте есть `not_applicable` |
| 22 | Якоря CV-метрик калибруются по перцентилям корпуса (`calibrate_anchors.py`) | Абсолютные значения не универсальны: Tenengrad зависит от гарнитуры, кегля и dpi. Якоря с синтетики дали на реальных сканах вырожденный скор (`blur` ≈ 0.98 у всех). Это шкала для предразметки, а не пороги — настоящие пороги считаются по PR-кривым в С6 |
| 23 | `cropped` — это «штрихи рассечены границей кадра», а не «нет полей» | Минимальное поле меряет расстояние текста до края кадра и предполагает, что вокруг листа есть фон сканера. Скан, подрезанный ровно по листу, полей не имеет при сохранном документе — на архивном корпусе старое определение давало `cropped` = 1.00 всему подряд |
| 24 | Покорпусные оверлеи `configs/corpora/*.yaml` поверх `base.yaml` | Один файл не может быть точкой правды для двух корпусов сразу: чернила на границе кадра — дефект для офисного скана и норма для архивного разворота. Оверлей задаёт только отличия, всё остальное наследуется и валидируется той же схемой |
| 25 | `low_resolution` считается по высоте строки в РАБОЧЕМ разрешении (отменяет №15) | После запрета апскейла вариант `source_*` раздувался у больших сканов, которые мы уменьшаем, и вносил ложный разброс: на архивном корпусе распределение стало двугорбым, медианный скор дефекта — 0.69 у нормальных страниц |
| 26 | Перед оценкой высоты строки убираются компоненты во всю сторону, профиль сглаживается | Тёмный край скана даёт чернила в каждой строке профиля: порог превышен везде, страница схлопывается в одну «строку» высотой в лист — так ломалось ~40% архивного корпуса. Без сглаживания профиль дрожит вокруг порога и одна строка распадается на несколько: медиана высоты падала с 32 px до 7.8 px |
| 27 | Не удалось найти строки — `low_resolution` не выдаётся | Нулевая высота строки означала бы «текст нечитаемо мелкий», то есть максимальный дефект, хотя измерить просто не удалось. Тот же принцип, что и с битональными сканами (№21) |
| 28 | Ручная разметка сокращена с 600–800 до 300, доли сплита — 0.2 / 0.3 / 0.5 | Реальные размеченные страницы дефицитны, а train всё равно набирается синтетикой поверх чистых эталонов. Отдавать их в train незачем — важнее размер отложенного теста: при 300 размеченных обычные 70/15/15 дали бы тест в 45 страниц вместо заложенных 150–200. Риск №2 («синтетика ≠ реальность») от этого растёт, поэтому тест только реальный, а цель разметки можно поднять позже: разметчик продолжает с места остановки |
| 29 | Эталоны под деградацию отбираются по CV-скору, но ТОЛЬКО из документов вне val и test | Иначе страница, попавшая в тест, вернётся в обучение в виде своей деградированной копии — та же бумага, тот же прогон сканера. Это риск утечки №1, просто через синтетику |

## 14. Открытые вопросы

- [x] Источник и лицензия датасета: Tobacco3482 (MIT) + YeniseiGovReports-TD (MIT),
      оба скачаны в `data/raw/`. Синтетические образцы для ручной проверки приложения —
      в `data/samples/`, сгенерированные фотографии — в `data/gen_photos/`;
      в разметку не идут ни те, ни другие.
- [ ] Оптимизация к С6, где время инференса — отдельная метрика: CV-метрики
      упираются не в вычисления, а в память. Пул из 10 процессов даёт всего
      двукратный прирост (2 стр/с против 1 стр/с в один поток) — значит шина
      насыщена. Метрики считают в `float64`; перевод в `float32` вдвое сократит
      трафик. Проверить замером до и после.
- [ ] Tobacco3482 содержит настоящие имена и адреса (документы табачных процессов).
      Для учебного проекта допустимо, но в публичный репозиторий данные не кладём —
      `data/` и так в `.gitignore`.
- [x] Многостраничность в Tobacco проверена: имена — это Bates-номера
      (`0000002770`), 3482 уникальных, явных серий «документ_страница» нет. Но у 119
      групп совпадают первые 7 цифр (до 5 файлов в группе), а подряд идущие
      Bates-номера — это соседние страницы одного дела. Значит сплит делаем **по
      группам общего 7-значного префикса**, а не по именам файлов: иначе страницы
      одного документа разъедутся между train и test (риск утечки №1).
- [ ] Нужна ли обработка многостраничных PDF как единого документа (вердикт по документу, а не по странице)?
- [ ] Требуется ли докеризация для сдачи?
- [ ] Формат пояснительной записки и дедлайн
